#include<iostream>
#include<vector>
#include<algorithm>
#include<cstring>
using namespace std;
int main(){
    char arr[100];
    cin.getline(arr, 100);
    cout << arr << endl;
    // char arr[100];
    // gets(arr);
    // puts(arr);
    return 0;
}